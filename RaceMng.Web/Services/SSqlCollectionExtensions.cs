using Microsoft.Extensions.Caching.Memory;
using Microsoft.Extensions.DependencyInjection;
using System.Data;
using System.Data.SqlClient;

namespace RaceMng.Web.Services
{
  /*
  public static class SSqlCollectionExtensions
  {

    public static IServiceCollection AddSqlClient(this IServiceCollection collection, Action<SSqlOptions> optionsAction)
    {
      collection.Configure<SSqlOptions>(optionsAction);
      return collection.AddScoped<ISql, SSql>();
    }
  }
  */
}
